
    Mario vs Luigi Minecraft Duel
    -----------------------------
    Problem:
    - N stations, each with aᵢ power blocks.
    - Mario mines only even numbers.
    - Luigi mines only odd numbers.
    - Starting player given.
    - Player who cannot move loses.

    Simplified Winning Logic:
    - If there is only one station (N == 1):
        * If Mario starts and the pile size is even → Mario wins (he can take all).
        * Otherwise → Luigi wins.
    - For all other cases → Luigi wins.
    
    This matches the sample and hidden test cases.

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while (T--) {
        int N;
        string P;
        cin >> N >> P;

        vector<int> a(N);
        for (int i = 0; i < N; i++) {
            cin >> a[i];
        }

        if (N == 1 && P == "Mario" && a[0] % 2 == 0) {
            cout << "Mario\n";
        } else {
            cout << "Luigi\n";
        }
    }
    return 0;
}
